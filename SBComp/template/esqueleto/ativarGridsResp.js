
$(document).ready(function () {
    $(window).resize(function () {
        resize();
    });
    resize();

    function resize() {
        if ($(window).width() >= 996) {

        }
        if ($(window).width() < 996) {

        }
        if ($(window).width() < 768) {

        }
        if ($(window).width() < 480) {

        }
    }
});

